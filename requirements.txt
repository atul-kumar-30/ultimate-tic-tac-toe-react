# Note: This is a Node.js (React) project. 
# All dependencies are officially managed in the `package.json` file.
# To install all dependencies, run: npm install

# Core Stack
react
react-dom
vite

# Database & Authentication
@supabase/supabase-js

# Styling
# (Vanilla CSS is used, no external libraries required)
